# PROBE 테스트 시나리오

## 테스트 시나리오 1: 기본 질의 및 답변

### Q1: "동부권역 1년 전력 사용량 알려줘"

**기대 결과:**
- Plan 생성: `query_type=trend`, `metric=usage_kwh`, `filters.region=["동부"]`, `time_range=last_n_months(n=12)`
- 답변: 동부권역의 1년간 전력 사용량 추이 분석 결과
- Evidence: 적용 필터(지역: 동부), 지표(전력 사용량), 기간(최근 12개월)
- 자세히 보기 버튼: "에너지 인텔리전스에서 자세히 보기" (클릭 시 필터 자동 적용)

**확인 사항:**
- 대화 기록에 사용자 질문과 PROBE 답변이 표시됨
- 중복 출력 없음 (별도 답변 섹션 없음)
- Evidence와 Links가 대화 기록 내에 표시됨
- 버튼 key 충돌 없음

---

## 테스트 시나리오 2: 후속 질의 (컨텍스트 연결)

### Q1: "동부권역 1년 전력 사용량 알려줘"
### Q2: "이어서, 전력사용량이 높은 국소 5개 알려줘 (최근 3개월)"

**기대 결과:**
- Q2가 Q1의 동부권역 컨텍스트를 유지
- Plan: `query_type=top_n`, `metric=usage_kwh`, `filters.region=["동부"]` (Q1에서 상속), `time_range=last_n_months(n=3)` (Q2에서 명시), `top_n=5`
- 답변: 동부권역의 최근 3개월 전력 사용량 상위 5개 국소

**확인 사항:**
- `plan_merge`가 정상 동작하여 동부권역 필터가 유지됨
- 기간만 3개월로 변경됨
- 대화 기록에 두 질문과 답변이 순서대로 표시됨

---

## 테스트 시나리오 3: 자세히 보기 버튼 동작

### Q: "동부권역 최근 2개월 전력사용량 상위 20개"

**기대 결과:**
- 답변 후 "자세히 보기" 버튼 표시
- 버튼 클릭 시:
  1. `st.session_state["probe_nav"]`에 필터 정보 저장
  2. `st.switch_page("pages/1_에너지_인텔리전스.py")` 호출
  3. 대상 페이지에서 필터 자동 적용 (지역: 동부, 기간: 최근 2개월)

**확인 사항:**
- 페이지 이동 성공
- 필터가 자동으로 적용된 화면 표시
- `probe_nav`가 정상적으로 읽혀서 필터가 세팅됨

---

## 테스트 시나리오 4: DuplicateElementKey 방지

### 동작:
1. 질문 입력 → 답변 확인
2. 페이지 스크롤/재실행
3. 히스토리 반복 렌더링

**기대 결과:**
- `StreamlitDuplicateElementKey` 에러 미발생
- 모든 버튼/위젯이 유니크 key 사용 (`message_id` 기반)
- Key 형식: `{message_id}_nav_link_{idx}`, `{message_id}_evidence`

**확인 사항:**
- 페이지 새로고침 후에도 에러 없음
- 히스토리 재렌더링 시에도 key 충돌 없음

---

## 테스트 시나리오 5: PROBE 명칭 일관성

**확인 사항:**
- 모든 UI 텍스트에서 "Copilot" → "PROBE"로 변경됨
- 헤더: "🤖 PYLON PROBE"
- 입력창: "PROBE에게 질문하세요"
- 답변: "🤖 PROBE: ..."
- app.py: "🤖 PROBE" 섹션

---

## 수동 테스트 절차

1. **앱 실행**
   ```bash
   streamlit run app.py
   ```

2. **시나리오 1 실행**
   - 메뉴에서 "자연어 질의" 클릭
   - Q1 입력 후 "질문하기" 클릭
   - 답변 확인 (중복 없음 확인)
   - "자세히 보기" 버튼 클릭 → 페이지 이동 및 필터 적용 확인

3. **시나리오 2 실행**
   - Q1 입력 후 답변 확인
   - Q2 입력 ("이어서, 전력사용량이 높은 국소 5개 알려줘 (최근 3개월)")
   - Q2 답변에서 동부권역 필터가 유지되었는지 확인

4. **시나리오 4 확인**
   - 여러 질문 입력
   - 페이지 새로고침 (F5)
   - 히스토리 스크롤
   - 에러 없음 확인

5. **시나리오 5 확인**
   - 모든 화면에서 "PROBE" 명칭 확인



