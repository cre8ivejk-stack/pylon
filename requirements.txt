# PYLON - SKT Network센터 Energy Operations Platform
# Python 3.10 required (Streamlit Cloud compatible)

# Core framework
streamlit>=1.30.0,<2.0.0

# LLM (optional) - 선택적 의존성이므로 설치 실패해도 앱은 동작함
openai>=1.0.0,<2.0.0

# Data processing
pandas>=2.0.3,<3.0.0
numpy>=1.24.3,<2.0.0

# Parquet support - fastparquet은 pure Python이라 빌드 불필요
fastparquet>=2023.10.0

# Visualization
plotly>=5.18.0,<6.0.0
altair>=5.0.0,<6.0.0

# Utilities
python-dateutil>=2.8.2
PyYAML>=6.0.0

# Development & Analysis (Jupyter) - REMOVED for Streamlit Cloud deployment
# Jupyter packages are not needed for Streamlit web app deployment

